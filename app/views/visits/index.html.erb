<p style="color: green"><%= notice %></p>

<h1>Visits</h1>

<div id="visits">
  <% @visits.each do |visit| %>
    <%= render visit %>
    <p>
      <%= link_to "Show this visit", visit %>
    </p>
  <% end %>
</div>
