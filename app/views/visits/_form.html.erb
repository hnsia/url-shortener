<%= form_with(model: visit) do |form| %>
  <% if visit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(visit.errors.count, "error") %> prohibited this visit from being saved:</h2>

      <ul>
        <% visit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :url_id, style: "display: block" %>
    <%= form.text_field :url_id %>
  </div>

  <div>
    <%= form.label :geolocation, style: "display: block" %>
    <%= form.text_field :geolocation %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
